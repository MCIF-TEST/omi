# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/

# Node / Next.js
node_modules/
.next/
.turbo/
out/
.DS_Store

# Environment
.env
.env.local
.env.production.local
*.env.local

# Runtime data
apps/api/data/
.docker-data/

# Dataset ingestion runtime artifacts (provenance is per-DB / per-run; the
# source datasets themselves are committed, these generated outputs are not)
datasets/.omi_ingest_ledger.json
datasets/_generated/
*.joblib

# Editor
.vscode/
.idea/
*.swp

# OS
Thumbs.db
apps/web/tsconfig.tsbuildinfo

# Fitted ML artifacts — environment-specific, produced by scripts/fit_correlation.py.
# A dev-fit committed here would silently change scoring behavior; deploy via the
# environment instead. Remove this line to ship a validated model.
apps/api/models/signal_correlation.json
